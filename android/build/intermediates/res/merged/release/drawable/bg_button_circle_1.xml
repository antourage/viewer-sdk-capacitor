<?xml version="1.0" encoding="utf-8"?>
<ripple xmlns:android="http://schemas.android.com/apk/res/android"
        android:color="?attr/colorControlHighlight">
    <!-- Use this to define the shape of the ripple effect. The color specified here isn't used -->
    <item android:id="@android:id/mask">
        <shape android:shape="oval">
            <!-- This color is needed to be set - doesn't affect anything -->
            <solid android:color="@android:color/white"/>
        </shape>
    </item>
    <!-- This is the background of button -->
    <item>
        <shape android:shape="oval">
            <stroke
                    android:width="1dp"
                    android:color="@color/ant_white_a60" />
        </shape>
    </item>
</ripple>